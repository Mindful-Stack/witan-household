{
  "name": "witan-lore-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Witan-household KB tooling: validate (frontmatter, links, orphans, tag health), doctor",
  "scripts": {
    "test": "node --test __tests__/*.test.js"
  }
}
